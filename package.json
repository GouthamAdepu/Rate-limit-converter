{
  "name": "token-bucket-rate-limiter",
  "version": "1.0.0",
  "description": "Node.js Express application with IP-based rate limiting using Token Bucket algorithm",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --verbose"
  },
  "jest": {
    "testEnvironment": "node",
    "verbose": true
  },
  "keywords": ["express", "rate-limiting", "token-bucket"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
